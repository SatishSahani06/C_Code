//wap to program check is:
#include "stdio.h"
int main()
{

    int count,i;
    char str[50];
    printf("Entyer string\n");
    gets(str);
    printf("find your string\n");
    for(int i=0;str[i]!='\0';i++)
    
        gets(str);
    {
    if(str[i]==' ' && str[i]=='i'&& str[i+1]=='i'||str[i+2]=='s' && str[i+3]==' ')

    

    {
    
        count++;
    }
    }

    printf("%c",str);

}