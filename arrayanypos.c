#include "stdio.h"
int main()
{
  /*  int arr[100],i,size,pos,item;
    printf("Enter size of array");
    scanf("%d",&size);
    printf("Enter element");
    for ( i = 0; i < size; i++)
   {
    scanf("%d",&arr[i]);        
   }
   printf("Display Array");
   for ( i = 0; i < size; i++)
   {
    printf(" %d ",arr[i]);  
    if(i <size-1)
    {
        printf(",");
    }   
   }
   printf("\nEnter any Position");
   scanf("%d",&pos);
   printf("Enter Item You Want");
   scanf("%d",&item);
   for(i=size;i>=pos;i--)
   {
     arr[i]=arr[i-1];
   } 
   arr[pos]=item;
   size++;

   printf("Display Array is ");
   for(i=0;i<size;i++)
   {
    printf(" %d",arr[i]);
    if(i<size-1)
    {
        printf(",");
    }
   }   
    */  
   int arr[5]={11,22,33,44,55} ;
   //for(int i=0;i<5;i++)
  // {
  //  printf(" %d ",arr[i]);

    // if(i <5-1)
   printf("%d",arr[2]);
    {
        printf(",");
    }
   //}     
}
