#include "stdio.h"
#include "ctype.h"
void main()
{
  int n;
    char str[n];
    int i=0;
    printf("Enter name:\n");
    gets(str);

      for(i=0;str[i]!='\0';i++)
    {
       if(str[i]=toupper(str[0]));
           printf("Upper case %s\n",str);

    }
  

}